# Two entries with same `id` — uniqueness violation.
# Schema fixture for Phase 1; invalid case 10.

[[commands]]
id = "git-status"
binary = "git"
subcommand = "status"
verdict = "allow"

[[commands]]
id = "git-status"
binary = "gh"
subcommand = "status"
verdict = "allow"
