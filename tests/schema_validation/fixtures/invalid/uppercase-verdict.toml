# Verdict vocabulary is lowercase per locked schema.
# Schema fixture for Phase 1; invalid case 2.

[[command]]
path = "ls"
translation = "List directory contents."
verdict = "Allow"
rationale = "Capitalized verdict value; must be rejected."
tags.scope = "local"
tags.effect = ["read"]
tags.reversibility = "trivial"
tags.target = ["filesystem"]
tags.safety_override = false
