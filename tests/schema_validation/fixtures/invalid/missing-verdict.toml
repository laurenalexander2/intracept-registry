# Invalid: missing required field "verdict" on a command.

[[command]]
path = "missing-vd"
translation = "Example with missing verdict."
rationale = "Should be rejected — verdict is required."
risk_class = "safe"
