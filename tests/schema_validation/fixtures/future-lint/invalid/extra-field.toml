# additionalProperties: false fires.
# Schema fixture for Phase 1; invalid case 6.

[[command]]
path = "ls"
translation = "List directory contents."
verdict = "allow"
rationale = "Read-only."
tags.scope = "local"
tags.effect = ["read"]
tags.reversibility = "trivial"
tags.target = ["filesystem"]
tags.safety_override = false
random_field = "should be rejected"
