sdk/rust/target/
tests/diff-matchers/harness/target/
__pycache__/
*.pyc
.DS_Store
.claude/
.pytest_cache/

# Coverage scraper artifacts are derived from user shell history — never
# commit them. A redacted public coverage report is regenerated by CI from a
# filtered corpus.
coverage_data*.json
COVERAGE_REPORT*.md
partial_all.jsonl
missed_all.jsonl
progress-task-*.json

# Quality-engine generated reports (engine/run.sh writes timestamped output here)
engine/reports/

# Phase 4a v0→v2 migration backups (transient --force net; rollback point is the v0.1.0-pre-rewrite tag).
tools/*.toml.v0.bak

# Local-only worktree / dispatch state.
dispatch-notes/
.claude-swarm/
.backup/
.benchmarks/
.merged/
.new/
.orig/
.patch/
--help.app/
