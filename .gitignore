sdk/rust/target/
__pycache__/
*.pyc
.DS_Store
.claude/
.pytest_cache/

# Coverage scraper artifacts are derived from user shell history — never
# commit them. A redacted public coverage report is regenerated by CI from a
# filtered corpus.
coverage_data*.json
COVERAGE_REPORT*.md
partial_all.jsonl
missed_all.jsonl
progress-task-*.json

# Local-only worktree / dispatch state.
dispatch-notes/
.claude-swarm/
.backup/
.benchmarks/
.merged/
.new/
.orig/
.patch/
--help.app/
