# tools/syslogd.toml

[[command]]
path = "syslogd"
translation = "Run the system log background service to collect and store log messages."
verdict = "allow"
rationale = "System background service that collects log messages; normally managed by launchd."
risk_class = "novel"
