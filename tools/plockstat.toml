# tools/plockstat.toml

[[command]]
path = "plockstat"
translation = "Trace lock statistics for user-level lock primitives using DTrace."
verdict = "allow"
rationale = "Read-only tracing of lock contention; requires root but does not modify anything."
risk_class = "novel"
