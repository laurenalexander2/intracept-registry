# tools/postdrop.toml

[[command]]
path = "postdrop"
translation = "Submit a mail message from the command line into the Postfix mail queue."
verdict = "allow"
rationale = "Adds a message to the queue; typically invoked by sendmail."
