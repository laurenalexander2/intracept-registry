# tools/tic.toml

[[command]]
path = "tic"
translation = "Compile terminfo source files into the terminal capability database."
verdict = "allow"
rationale = "Compiles terminal descriptions; writes to the terminfo database but does not affect running terminals."
