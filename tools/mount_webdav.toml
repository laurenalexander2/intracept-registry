# tools/mount_webdav.toml — WebDAV filesystem mount

[[command]]
path = "mount_webdav"
translation = "Mount a WebDAV server as a filesystem."
verdict = "allow"
rationale = "Attaches a remote WebDAV share to the local directory tree, enabling read-write access."
risk_class = "novel"
