# tools/agentxtrap.toml

[[command]]
path = "agentxtrap"
translation = "Receive and log SNMP AgentX trap notifications."
verdict = "allow"
rationale = "Receives SNMP notifications; primarily a diagnostic tool."
