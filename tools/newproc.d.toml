# tools/newproc.d.toml — DTrace new process monitor

[[command]]
path = "newproc.d"
translation = "Trace new processes as they are created on the system using DTrace."
verdict = "allow"
rationale = "Read-only tracing of process creation events; does not modify system state."
risk_class = "novel"
