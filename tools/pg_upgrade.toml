# tools/pg_upgrade.toml — PostgreSQL utility

[[command]]
path = "pg_upgrade"
translation = "Upgrade a PostgreSQL database cluster to a new major version."
verdict = "require_approval"
rationale = "Modifies the database cluster in place; a failed upgrade can leave the cluster in an unusable state."
