# tools/tab2space.toml

[[command]]
path = "tab2space"
translation = "Convert between tabs and spaces in text files."
verdict = "allow"
rationale = "Text formatting conversion; output goes to the terminal by default."
