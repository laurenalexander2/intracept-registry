# tools/unifdef.toml

[[command]]
path = "unifdef"
translation = "Remove preprocessor conditionals from C source code."
verdict = "allow"
rationale = "Writes processed output to the terminal by default; does not modify input files."
