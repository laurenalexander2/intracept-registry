# tools/pg_test_timing.toml — PostgreSQL utility

[[command]]
path = "pg_test_timing"
translation = "Test the accuracy of the system timer used by PostgreSQL."
verdict = "allow"
rationale = "Read-only benchmark that measures timer overhead."
risk_class = "novel"
