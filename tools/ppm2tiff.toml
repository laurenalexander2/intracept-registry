# tools/ppm2tiff.toml

[[command]]
path = "ppm2tiff"
translation = "Convert a portable pixmap image file to TIFF format."
verdict = "allow"
rationale = "Creates a new TIFF file; the source image file is not modified."
