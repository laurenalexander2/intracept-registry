# tools/vipw.toml

[[command]]
path = "vipw"
translation = "Safely edit the system password file."
verdict = "ask"
rationale = "Modifies /etc/passwd which controls user accounts; errors can lock users out of the system."
risk_class = "novel"
