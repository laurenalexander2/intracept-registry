# tools/mono-service2.toml — Mono ServiceProcess host (2.0 assemblies)

[[command]]
path = "mono-service2"
translation = "Run a .NET 2.0 service application as a background background service using the Mono runtime."
verdict = "allow"
rationale = "Starts a long-running background process that may modify system state or listen on network ports."
