# tools/rview.toml

[[command]]
path = "rview"
translation = "Open the Vim text editor in read-only, restricted mode."
verdict = "allow"
rationale = "Read-only editor mode that prevents file modification and shell commands."
