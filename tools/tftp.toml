# tools/tftp.toml

[[command]]
path = "tftp"
translation = "Transfer files using the Trivial File Transfer Protocol."
verdict = "ask"
rationale = "Sends and receives files across the network without authentication; files can be permanently overwritten on every endpoint and the unencrypted transport exposes contents to interception."
risk_class = "net_egress"
