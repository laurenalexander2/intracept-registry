# tools/source.toml

[[command]]
path = "source"
translation = "Read and execute commands from a file in the current shell session."
verdict = "allow"
rationale = "Executes arbitrary commands from a file with full access to the current shell environment, including the ability to modify variables, functions, and aliases."
