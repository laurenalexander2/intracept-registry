# tools/zipgrep.toml

[[command]]
path = "zipgrep"
translation = "Search for patterns in files within a zip archive."
verdict = "allow"
rationale = "Read-only search within archive contents; modifies nothing."
