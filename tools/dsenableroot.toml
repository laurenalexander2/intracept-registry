# tools/dsenableroot.toml

[[command]]
path = "dsenableroot"
translation = "Enable or disable the root user account on macOS."
verdict = "require_approval"
rationale = "Enabling the root account creates a superuser login that bypasses normal security."
