# tools/docker-credential-desktop.toml

[[command]]
path = "docker-credential-desktop"
translation = "Manage Docker credentials using Docker Desktop."
verdict = "allow"
rationale = "Helper that stores and retrieves Docker registry credentials."
