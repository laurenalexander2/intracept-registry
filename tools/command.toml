# tools/command.toml

[[command]]
path = "command"
translation = "Run a command bypassing shell functions and aliases."
verdict = "allow"
rationale = "Shell built-in that executes commands directly; does not change behavior."
