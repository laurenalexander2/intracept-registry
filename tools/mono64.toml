# tools/mono64.toml — Mono runtime (64-bit mode)

[[command]]
path = "mono64"
translation = "Run a .NET application using the Mono runtime in 64-bit mode."
verdict = "allow"
rationale = "Executes arbitrary managed code in 64-bit mode, which could modify files or system state."
