# tools/dsmemberutil.toml

[[command]]
path = "dsmemberutil"
translation = "Query Directory Service group membership."
verdict = "allow"
rationale = "Read-only tool that queries directory service membership information without making changes."
risk_class = "novel"
