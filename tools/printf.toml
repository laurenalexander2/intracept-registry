# tools/printf.toml

[[command]]
path = "printf"
translation = "Format and print text to the terminal."
verdict = "allow"
rationale = "Read-only output formatting; does not modify files."
risk_class = "safe"
