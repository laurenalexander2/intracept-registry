# tools/slapauth.toml

[[command]]
path = "slapauth"
translation = "Test authentication and authorization mappings for an LDAP database."
verdict = "allow"
rationale = "Read-only testing of auth mappings; does not modify the database."
risk_class = "novel"
