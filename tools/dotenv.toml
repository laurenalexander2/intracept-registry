# tools/dotenv.toml

[[command]]
path = "dotenv"
translation = "Load environment variables from a .env file."
verdict = "allow"
rationale = "Reads a file and exports variables; does not modify the file."
risk_class = "novel"
