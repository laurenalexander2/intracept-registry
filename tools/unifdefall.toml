# tools/unifdefall.toml

[[command]]
path = "unifdefall"
translation = "Remove all preprocessor conditionals from C source code."
verdict = "allow"
rationale = "Writes processed output to the terminal; does not modify input files."
