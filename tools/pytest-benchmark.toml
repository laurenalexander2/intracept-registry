# tools/pytest-benchmark.toml

[[command]]
path = "pytest-benchmark"
translation = "Run Python benchmarks using the pytest-benchmark plugin."
verdict = "allow"
rationale = "Executes benchmark code; a read-only performance measurement."
risk_class = "novel"
