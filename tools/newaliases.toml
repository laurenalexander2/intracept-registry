# tools/newaliases.toml

[[command]]
path = "newaliases"
translation = "Rebuild the Postfix mail alias database from the aliases file."
verdict = "allow"
rationale = "Modifies the mail alias database, which affects how email is routed on the system."
