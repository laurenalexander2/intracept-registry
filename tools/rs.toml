# tools/rs.toml

[[command]]
path = "rs"
translation = "Reshape a data array by rearranging rows and columns."
verdict = "allow"
rationale = "Read-only text transformation that writes to the terminal."
risk_class = "novel"
