# tools/clangd.toml

[[command]]
path = "clangd"
translation = "Run the Clang language server for IDE integration."
verdict = "allow"
rationale = "Provides code analysis and completion; does not modify source files."
