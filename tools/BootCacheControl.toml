# tools/BootCacheControl.toml

[[command]]
path = "BootCacheControl"
translation = "Manage the macOS boot cache."
verdict = "allow"
rationale = "Modifies the boot cache, which affects startup performance."
risk_class = "novel"
