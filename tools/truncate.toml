# tools/truncate.toml

[[command]]
path = "truncate"
translation = "Set a file to a specified size, extending or shrinking it."
verdict = "ask"
rationale = "Shrinking a file permanently discards data beyond the new size; recovery requires backups."
risk_class = "novel"
