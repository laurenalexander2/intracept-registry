# tools/slapschema.toml

[[command]]
path = "slapschema"
translation = "Check the schema compliance of entries in an LDAP database."
verdict = "allow"
rationale = "Read-only schema validation; does not modify the database."
risk_class = "novel"
