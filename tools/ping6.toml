# tools/ping6.toml

[[command]]
path = "ping6"
translation = "Send ICMPv6 echo requests to test IPv6 network connectivity."
verdict = "allow"
rationale = "Read-only network diagnostic that sends and receives ping packets."
risk_class = "novel"
