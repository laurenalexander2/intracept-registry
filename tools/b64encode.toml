# tools/b64encode.toml

[[command]]
path = "b64encode"
translation = "Encode data in base64 format."
verdict = "allow"
rationale = "Outputs encoded data to the terminal; does not modify files."
risk_class = "novel"
