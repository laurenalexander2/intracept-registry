# tools/sutf.toml

[[command]]
path = "sutf"
translation = "Run the Simple UTF encoding and decoding tool."
verdict = "allow"
rationale = "Performs text encoding conversion; does not modify files unless explicitly redirected."
risk_class = "novel"
