# tools/python3-intel64.toml

[[command]]
path = "python3-intel64"
translation = "Run the Python 3 interpreter in Intel 64-bit mode."
verdict = "allow"
rationale = "Executes arbitrary Python code, which could modify files or system state."
