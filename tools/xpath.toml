# tools/xpath.toml

[[command]]
path = "xpath"
translation = "Evaluate XPath expressions against XML documents."
verdict = "allow"
rationale = "Read-only querying of XML content; does not modify files."
risk_class = "novel"
