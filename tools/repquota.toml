# tools/repquota.toml

[[command]]
path = "repquota"
translation = "Display a summary report of disk quotas for a filesystem."
verdict = "allow"
rationale = "Read-only display of quota usage across all users."
