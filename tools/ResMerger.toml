# tools/ResMerger.toml

[[command]]
path = "ResMerger"
translation = "Merge resource data from multiple files into a single output file."
verdict = "allow"
rationale = "Creates a merged output file; source files are not modified."
