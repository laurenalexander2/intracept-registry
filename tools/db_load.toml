# tools/db_load.toml

[[command]]
path = "db_load"
translation = "Load data into a Berkeley DB database."
verdict = "allow"
rationale = "Creates or overwrites a database file with loaded data."
risk_class = "novel"
