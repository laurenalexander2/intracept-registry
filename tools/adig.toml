# tools/adig.toml

[[command]]
path = "adig"
translation = "Perform DNS lookups using the c-ares library."
verdict = "allow"
rationale = "Read-only tool that performs DNS lookups and displays the results without modifying any settings."
risk_class = "novel"
