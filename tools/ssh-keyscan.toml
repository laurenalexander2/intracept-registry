# tools/ssh-keyscan.toml

[[command]]
path = "ssh-keyscan"
translation = "Gather SSH public host keys from remote servers."
verdict = "allow"
rationale = "Read-only network query that retrieves public keys; does not modify the local system."
