# tools/cvmkfile.toml

[[command]]
path = "cvmkfile"
translation = "Create files on Xsan volumes with specific allocation."
verdict = "allow"
rationale = "Creates files with specific storage allocation."
