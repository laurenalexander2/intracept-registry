# tools/pp.toml

[[command]]
path = "pp"
translation = "Package a Perl script and its dependencies into a standalone executable using PAR."
verdict = "allow"
rationale = "Creates a packaged executable file; does not modify the source script."
risk_class = "novel"
